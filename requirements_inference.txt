# Requirements for RunPod Serverless Inference
# Excludes training-only and unused packages to reduce image size and build time
# Based on Hunyuan3D-2.1 requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu121

# PyTorch - NOT listed here, installed separately in Dockerfile to avoid
# double-downloading 780MB+ with --ignore-installed
# torch==2.5.1+cu121, torchvision==0.20.1+cu121

# Build Tools
ninja==1.11.1.1
pybind11==2.13.4

# Core ML/Deep Learning
transformers==4.46.0
diffusers==0.30.0
accelerate==1.1.1
# pytorch-lightning: required - Omni model extends pl.LightningModule,
# Paint UNet model extends pl.LightningModule
pytorch-lightning==2.2.0
huggingface-hub==0.30.2
safetensors==0.4.4

# Scientific Computing
numpy==1.26.4
scipy==1.14.1
einops==0.8.0
# pandas - REMOVED: not imported anywhere in inference code

# Computer Vision & Image Processing
opencv-python==4.10.0.84
opencv-python-headless==4.10.0.84  # Pin to avoid rembg downloading multiple versions
imageio==2.36.0
scikit-image==0.24.0
rembg==2.0.65

# 3D Mesh Processing
trimesh==4.4.7
pymeshlab==2023.12.post3
pygltflib==1.16.3
xatlas==0.0.9
# open3d - REMOVED: 447MB + 20 transitive deps, not imported anywhere

# Configuration Management
omegaconf==2.3.0
pyyaml==6.0.2
configargparse==1.7

# Web Framework - REMOVED: RunPod SDK has its own server, not used by handler
# fastapi==0.115.12
# uvicorn==0.34.3

# Utilities
tqdm==4.66.5
psutil==6.0.0

# S3 Storage (for large output files)
boto3>=1.26.0

# GPU Computing
# cupy-cuda12x - REMOVED: 104MB, not imported anywhere in inference code

# ONNX Runtime (GPU acceleration for RealESRGAN upscaler)
onnxruntime-gpu==1.19.2
torchmetrics==1.6.0

pydantic==2.10.6

# timm: required - moe_layers.py imported by hunyuandit.py denoiser
timm
# torchdiffeq: required - odeint used in ODE sampling (Omni pipeline)
torchdiffeq
# pythreejs - REMOVED: Jupyter 3D widget, not used in serverless
# torchaudio - REMOVED: not used in image-to-3D pipeline
